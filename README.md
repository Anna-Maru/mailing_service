# 📮 Сервис Email-рассылок

> Django-приложение для автоматизации email-рассылок с системой управления клиентами и аналитикой
---

## 📋 Содержание

- [О проекте](#о-проекте)
- [Функциональность](#функциональность)
- [Технологии](#технологии)
- [Установка](#установка)
- [Настройка](#настройка)
- [Использование](#использование)
- [Архитектура](#архитектура)
- [API и команды](#api-и-команды)
- [Тестирование](#тестирование)
- [Авторы](#авторы)

---

## 🎯 О проекте

Веб-приложение для управления email-рассылками с возможностью создания клиентской базы, настройки расписания отправки и детальной аналитикой доставки сообщений.

**Ключевые возможности:**
- 📧 Создание и управление email-рассылками
- 👥 База клиентов с возможностью сегментации
- 📊 Детальная статистика отправок
- 🔐 Система ролей и прав доступа
- ⚡ Кеширование для оптимизации производительности

---

## ✨ Функциональность

### Для пользователей:
- Регистрация с подтверждением email
- Управление личным профилем
- Создание и редактирование клиентской базы
- Создание шаблонов сообщений
- Настройка рассылок с указанием времени старта/окончания
- Просмотр истории отправок
- Персональная статистика (успешные/неуспешные отправки)

### Для менеджеров:
- Просмотр всех пользователей системы
- Просмотр всех рассылок и клиентов
- Блокировка пользователей
- Деактивация рассылок
- Доступ к общей статистике

### Для администраторов:
- Полный доступ через Django Admin
- Управление группами и правами
- Модерация контента

---

## 🛠 Технологии

### Backend:
- **Python 3.10+**
- **Django 5.0** - web framework
- **PostgreSQL 15+** - основная СУБД
- **Redis** - кеширование (опционально, можно использовать LocMemCache)

### Frontend:
- **Bootstrap 5** - UI framework
- **HTML/CSS** - вёрстка
- **Django Templates** - шаблонизатор

### Дополнительно:
- **python-dotenv** - управление переменными окружения
- **Pillow** - обработка изображений (аватары)
- **psycopg2-binary** - PostgreSQL adapter

---

## 📦 Установка

### Предварительные требования:

- Python 3.10 или выше
- PostgreSQL 15 или выше
- Git

### Шаг 1: Клонирование репозитория

```bash
git clone https://github.com/yourusername/mailing_service.git
cd mailing_service
```

### Шаг 2: Создание виртуального окружения

```bash
# Windows
python -m venv .venv
.venv\Scripts\activate

# macOS/Linux
python3 -m venv .venv
source .venv/bin/activate
```

### Шаг 3: Установка зависимостей

```bash
pip install -r requirements.txt
```

---

## ⚙️ Настройка

### 1. База данных PostgreSQL

Создайте базу данных:

```sql
CREATE DATABASE mailing_service_db;
CREATE USER mailing_user WITH PASSWORD 'your_password';
ALTER ROLE mailing_user SET client_encoding TO 'utf8';
ALTER ROLE mailing_user SET default_transaction_isolation TO 'read committed';
ALTER ROLE mailing_user SET timezone TO 'UTC';
GRANT ALL PRIVILEGES ON DATABASE mailing_service_db TO mailing_user;
```

### 2. Переменные окружения

Создайте файл `.env` в корне проекта:

```env
# Database
NAME=mailing_service_db
USER=mailing_user
PASSWORD=your_password
HOST=localhost
PORT=5432

# Django
SECRET_KEY=your-secret-key-here
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Email (опционально, для реальной отправки)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-app-password
```

**Генерация SECRET_KEY:**
```python
python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
```

### 3. Применение миграций

```bash
python manage.py makemigrations
python manage.py migrate
```

### 4. Создание суперпользователя

```bash
python manage.py createsuperuser
```

**Важно:** Вход осуществляется по **email**, а не по username!

### 5. Создание группы "Менеджеры"

```bash
python manage.py create_manager_group
```

### 6. (Опционально) Создание тестовых данных

```bash
# Тестовые пользователи
python manage.py create_test_users

# Или назначение владельцев существующим объектам
python manage.py assign_owners
```

---

## 🚀 Использование

### Запуск сервера разработки

```bash
python manage.py runserver
```

Приложение будет доступно по адресу: http://127.0.0.1:8000/

### Основные URL:

| URL | Описание |
|-----|----------|
| `/` | Главная страница |
| `/users/register/` | Регистрация |
| `/users/login/` | Вход (по email!) |
| `/users/profile/` | Профиль пользователя |
| `/clients/` | Список клиентов |
| `/messages/` | Список сообщений |
| `/mailings/` | Список рассылок |
| `/statistics/` | Статистика пользователя |
| `/admin/` | Административная панель |

### Management команды

```bash
# Отправка рассылки вручную
python manage.py send_mailings <ID>

# Просмотр всех рассылок
python manage.py list_mailings

# Создание группы менеджеров
python manage.py create_manager_group

# Создание тестовых пользователей
python manage.py create_test_users

# Назначение владельцев объектам
python manage.py assign_owners
```

---

## 🏗 Архитектура

### Структура проекта

```
mailing_service/
├── config/                 # Настройки проекта
│   ├── settings.py        # Основные настройки
│   ├── urls.py            # Главный роутинг
│   └── wsgi.py
├── users/                 # Приложение пользователей
│   ├── models.py          # User (расширенная модель)
│   ├── views.py           # Регистрация, вход, профиль
│   ├── forms.py           # Формы аутентификации
│   └── management/
│       └── commands/      # Команды создания пользователей
├── mailings/              # Приложение рассылок
│   ├── models.py          # Client, Message, Mailing, MailingAttempt
│   ├── views.py           # CRUD для рассылок
│   ├── forms.py           # Формы создания/редактирования
│   └── management/
│       └── commands/      # Команды отправки, создания групп
├── templates/             # Шаблоны
│   └── mailings/
│       ├── base.html
│       ├── home.html
│       └── ...
├── static/                # Статические файлы (опционально)
├── media/                 # Загружаемые файлы (аватары)
├── .env                   # Переменные окружения
├── requirements.txt       # Зависимости
└── manage.py
```

### Модели данных

#### User (users/models.py)
```python
- email (EmailField, unique, используется для входа)
- role (CharField: user/manager/admin)
- phone (CharField)
- country (CharField)
- avatar (ImageField)
- is_email_verified (BooleanField)
- is_blocked (BooleanField)
```

#### Client (mailings/models.py)
```python
- email (EmailField)
- full_name (CharField)
- comment (TextField)
- owner (ForeignKey -> User)
```

#### Message (mailings/models.py)
```python
- subject (CharField)
- body (TextField)
- created_at (DateTimeField)
- owner (ForeignKey -> User)
```

#### Mailing (mailings/models.py)
```python
- start_time (DateTimeField)
- end_time (DateTimeField)
- status (CharField: Создана/Запущена/Завершена)
- message (ForeignKey -> Message)
- recipients (ManyToManyField -> Client)
- owner (ForeignKey -> User)
- is_active (BooleanField)
```

#### MailingAttempt (mailings/models.py)
```python
- mailing (ForeignKey -> Mailing)
- attempt_time (DateTimeField)
- status (CharField: Успешно/Не успешно)
- server_response (TextField)
- recipient_email (EmailField)
```

---

## 🔐 Система прав доступа

### Роли:

| Роль | Права |
|------|-------|
| **Пользователь** | CRUD своих клиентов, сообщений, рассылок. Просмотр своей статистики |
| **Менеджер** | Просмотр всех данных. Блокировка пользователей. Деактивация рассылок. **НЕ может редактировать чужие данные** |
| **Администратор** | Полный доступ через Django Admin |

### Permissions в моделях:

```python
# Client
permissions = [
    ('can_view_all_clients', 'Может просматривать всех клиентов'),
    ('can_disable_client', 'Может отключать клиентов'),
]

# Message
permissions = [
    ('can_view_all_messages', 'Может просматривать все сообщения'),
    ('can_disable_message', 'Может отключать сообщения'),
]

# Mailing
permissions = [
    ('can_view_all_mailings', 'Может просматривать все рассылки'),
    ('can_disable_mailing', 'Может отключать рассылки'),
]
```

---

## 📊 Кеширование

Настроено кеширование для оптимизации производительности:

### Кешируемые страницы:

- **Главная страница** (`/`) - 5 минут
- **Статистика пользователя** (`/statistics/`) - 5 минут

### Конфигурация (settings.py):

```python
# Вариант 1: Redis (рекомендуется для продакшена)
CACHES = {
    'default': {
        'BACKEND': 'django.core.cache.backends.redis.RedisCache',
        'LOCATION': 'redis://127.0.0.1:6379/1',
    }
}

# Вариант 2: LocMemCache (для разработки)
CACHES = {
    'default': {
        'BACKEND': 'django.core.cache.backends.locmem.LocMemCache',
        'LOCATION': 'unique-snowflake',
        'TIMEOUT': 300,
    }
}
```

### Использование:

```python
from django.views.decorators.cache import cache_page
from django.utils.decorators import method_decorator

@method_decorator(cache_page(300), name='dispatch')
class HomeView(TemplateView):
    # ...
```

---

## 🧪 Тестирование

### Запуск тестов

```bash
python manage.py test
```

### Проверка кода линтером

```bash
flake8 .
```

### Тестовые пользователи

После выполнения `python manage.py create_test_users`:

| Username | Email | Password | Роль |
|----------|-------|----------|------|
| user1 | user1@test.com | password123 | Пользователь |
| user2 | user2@test.com | password123 | Пользователь |
| manager | manager@test.com | password123 | Менеджер |
| admin | admin@test.com | password123 | Администратор |

### Сценарии тестирования

1. **Регистрация и вход:**
   - Создать нового пользователя
   - Подтвердить email (в разработке - вручную через shell или админку)
   - Войти по email

2. **CRUD операции:**
   - Создать клиента
   - Создать сообщение
   - Создать рассылку с указанием времени
   - Отредактировать/удалить

3. **Права доступа:**
   - Войти как user1, создать данные
   - Войти как user2, убедиться что не видит данные user1
   - Войти как manager, убедиться что видит все данные
   - Попытаться отредактировать чужие данные - должна быть ошибка

4. **Отправка рассылок:**
   - Создать рассылку с текущим временем
   - Открыть детальную страницу
   - Нажать кнопку "Отправить"
   - Проверить историю попыток

---

